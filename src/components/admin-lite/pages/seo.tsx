'use client';

import { AdminCard, AdminShell, Field, SectionTitle, createChangeHandler, fieldClassName, textareaClassName, useAdmin, type DemoSeoRecord } from '../shared';

export function AdminSeoPage() { return <AdminShell active="seo" title="SEO" description="Мета-заголовки, описания и robots для страниц."><SeoContent /></AdminShell>; }
function SeoContent() { const { data, updateData } = useAdmin(); function updateSeo<K extends keyof DemoSeoRecord>(id: string, key: K, value: DemoSeoRecord[K]) { updateData((c) => ({ ...c, seo: createChangeHandler(c.seo, id, key, value) }), 'Изменено SEO', 'SEO'); } return <><SectionTitle eyebrow="SEO" title="Мета-данные страниц" /><div className="grid gap-4">{data.seo.map((item) => <AdminCard key={item.id} className="grid gap-4"><div className="grid gap-4 md:grid-cols-2"><Field label="Страница"><input className={fieldClassName()} value={item.page} onChange={(event) => updateSeo(item.id, 'page', event.target.value)} /></Field><Field label="Robots"><select className={fieldClassName()} value={item.robots} onChange={(event) => updateSeo(item.id, 'robots', event.target.value as DemoSeoRecord['robots'])}><option>index, follow</option><option>noindex, nofollow</option></select></Field></div><Field label="Title"><input className={fieldClassName()} value={item.title} onChange={(event) => updateSeo(item.id, 'title', event.target.value)} /></Field><Field label="Description"><textarea className={textareaClassName('min-h-24')} value={item.description} onChange={(event) => updateSeo(item.id, 'description', event.target.value)} /></Field><Field label="OG title"><input className={fieldClassName()} value={item.ogTitle} onChange={(event) => updateSeo(item.id, 'ogTitle', event.target.value)} /></Field></AdminCard>)}</div></>; }
