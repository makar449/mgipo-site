'use client';

import { AdminCard, AdminShell, Field, SectionTitle, StatusPill, createChangeHandler, fieldClassName, statusTone, useAdmin, type DemoPageRecord } from '../shared';

export function AdminPagesPage() { return <AdminShell active="pages" title="Страницы" description="Карта сайта и статус публикации страниц."><PagesContent /></AdminShell>; }
function PagesContent() { const { data, updateData } = useAdmin(); function updatePage<K extends keyof DemoPageRecord>(id: string, key: K, value: DemoPageRecord[K]) { updateData((c) => ({ ...c, pages: createChangeHandler(c.pages, id, key, value) }), 'Изменена страница', 'Страницы'); } return <><SectionTitle eyebrow="Карта сайта" title="Публичные страницы" /><div className="grid gap-4">{data.pages.map((page) => <AdminCard key={page.id}><div className="grid gap-4 md:grid-cols-[1fr_1fr_0.45fr_0.5fr]"><Field label="Название"><input className={fieldClassName()} value={page.title} onChange={(event) => updatePage(page.id, 'title', event.target.value)} /></Field><Field label="URL"><input className={fieldClassName()} value={page.href} onChange={(event) => updatePage(page.id, 'href', event.target.value)} /></Field><div className="grid gap-2"><span className="text-xs font-black uppercase tracking-[0.16em] text-slate-600">Статус</span><StatusPill tone={statusTone(page.status)}>{page.status}</StatusPill></div><div className="grid gap-2"><span className="text-xs font-black uppercase tracking-[0.16em] text-slate-600">SEO</span><span className="rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-black text-slate-700">{page.seoStatus}</span></div></div></AdminCard>)}</div></>; }
