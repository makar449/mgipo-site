'use client';

import { AdminCard, AdminShell, Field, SectionTitle, fieldClassName, useAdmin } from '../shared';

export function AdminSettingsPage() { return <AdminShell active="settings" title="Настройки" description="Общие настройки демонстрационного режима и подготовки к Supabase."><SettingsContent /></AdminShell>; }
function SettingsContent() { const { data, updateData } = useAdmin(); return <><SectionTitle eyebrow="Система" title="Настройки проекта" /><div className="grid gap-5 lg:grid-cols-2"><AdminCard><Field label="Режим публикации"><select className={fieldClassName()} value={data.publicationMode} onChange={(event) => updateData((c) => ({ ...c, publicationMode: event.target.value as typeof c.publicationMode }), 'Изменён режим публикации', 'Настройки')}><option>Показ заказчику</option><option>Подготовка</option><option>Готово к Supabase</option></select></Field><p className="mt-4 text-sm font-semibold leading-7 text-slate-600">Сейчас панель работает в демо-режиме через localStorage. После Supabase эти же разделы можно подключить к общей базе.</p></AdminCard><AdminCard className="bg-blue-50/70"><h2 className="text-2xl font-black text-slate-950">Производительность</h2><p className="mt-3 text-sm font-semibold leading-7 text-slate-600">Админка разделена на отдельные маршруты. Публичный сайт и панель не должны грузиться одним общим экраном.</p></AdminCard></div></>; }
